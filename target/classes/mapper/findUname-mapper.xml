<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cal.mapper.GetUnameMapper">

    <select id="getUnameBysession" parameterType="java.lang.String" resultType="String">
      SELECT uname from sign WHERE session=#{sessionid};
    </select>
</mapper>

