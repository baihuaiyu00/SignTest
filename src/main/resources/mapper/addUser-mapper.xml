<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cal.mapper.AddUserMapper">

    <select id="addUser" parameterType="Member">
        INSERT INTO sign(uname,uid,session) VALUES (#{sname},#{sid},#{session})
    </select>
</mapper>

