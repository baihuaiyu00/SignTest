<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cal.mapper.SignConditionMapper">

    <select id="signcond" parameterType="Tion">
        INSERT INTO tion(signtime,signrank,uname) VALUES (#{signtime},#{signrank},#{uname})
    </select>
</mapper>

